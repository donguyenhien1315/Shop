{
  "name": "cantin-ai-standalone",
  "version": "2.1.0",
  "private": true,
  "type": "module",
  "description": "Quản lý căn tin độc lập, chạy trên Node.js/PostgreSQL hoặc lưu file cục bộ; không phụ thuộc Replit.",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "check": "node --check server.js && node --check public/app.js && node --check src/store.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "pg": "^8.13.1"
  }
}
